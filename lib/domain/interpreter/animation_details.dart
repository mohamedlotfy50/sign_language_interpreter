class AnimationDetails {
  final int row, startingFrame, endingFrame;
  final bool isRepeating;

  AnimationDetails({
    required this.row,
    required this.startingFrame,
    required this.endingFrame,
    required this.isRepeating,
  });
}
